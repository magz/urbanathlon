<%= form_for(@workout_tag) do |f| %>
  <% if @workout_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_tag.errors.count, "error") %> prohibited this workout_tag from being saved:</h2>

      <ul>
      <% @workout_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
